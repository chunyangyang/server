<view>
  <form bindsubmit='bindSubmit'>
    <textarea 
    placeholder='请将您的建议告诉我' 
    auto-focus="{{autoFocus}}" 
    maxlength="{{maxlength}}"
    bindinput="bindInput"
    name="textarea"
    ></textarea>
     <button form-type='submit' disabled='{{disabled}}' >提交</button>
  </form>
</view>