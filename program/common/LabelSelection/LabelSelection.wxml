<view class="Selection">
  <block wx:for="{{list}}" wx:key="id">
    <view class="border {{item.isSelected?'active':''}}"  bindtap='bindClick'  name="select"  data-index="{{index}}" data-id="{{item.id}}">{{item.name}}</view>
  </block>
</view>
